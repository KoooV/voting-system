# Отчёты статического анализа кода

## Структура отчётов

```
reports/analysis/
├── initial/          # Отчёты первого анализа (до внесения ошибок)
│   ├── spotbugs/     # Отчёты SpotBugs
│   ├── pmd/          # Отчёты PMD
│   └── jacoco/       # Отчёты JaCoCo (покрытие кода)
├── with-errors/      # Отчёты второго анализа (после внесения ошибок)
│   ├── spotbugs/     # Отчёты SpotBugs
│   ├── pmd/          # Отчёты PMD
│   └── jacoco/       # Отчёты JaCoCo (покрытие кода)
├── ANALYSIS_REPORT.md           # Общий отчёт об анализе
└── COMPARATIVE_ANALYSIS.md      # Сравнительный анализ результатов
```

## Используемые инструменты

1. **SpotBugs** v4.8.3 - анализ байт-кода
2. **PMD** v7.6.0 - анализ исходного кода
3. **JaCoCo** v0.8.12 - анализ покрытия кода

## Результаты

### Первый анализ (до внесения ошибок)
- **SpotBugs**: Минимальное количество проблем
- **PMD**: 49 нарушений (стиль кода, best practices)
- **JaCoCo**: [см. отчёт]

### Второй анализ (после внесения 5 ошибок)
- **SpotBugs**: 8 проблем (обнаружена ошибка #5 - утечка памяти)
- **PMD**: 53 нарушения (обнаружены ошибки #3 и #5)
- **JaCoCo**: [см. отчёт]

## Внесённые ошибки

1. Логическая ошибка с null в `PollService.vote()` - **НЕ обнаружена**
2. Неиспользуемая переменная в `PollService.vote()` - **НЕ обнаружена явно**
3. Пустой catch блок в `PollController.vote()` - **Обнаружена PMD ✓**
4. Логическая ошибка с проверкой null в `Poll.registerVote()` - **НЕ обнаружена**
5. Потенциальная утечка памяти в `Poll.registerVote()` - **Обнаружена SpotBugs и PMD ✓✓**

## Выводы

- Статический анализ эффективен для обнаружения стилистических проблем и очевидных багов
- Логические ошибки сложно обнаружить статически
- Комбинация инструментов повышает вероятность обнаружения проблем
- Интеграция в CI/CD позволяет находить проблемы на ранних этапах

Подробнее см. `COMPARATIVE_ANALYSIS.md`

